# Keep Django on the latest 6.0.x patch to receive security fixes
# automatically.  Pinning a range (>= … , < next major) ensures
# compatible upgrades without unexpected breaking changes.
Django>=6.0.6,<6.1
django-environ>=0.11,<1
gunicorn>=23.0,<24
boto3>=1.35,<2
django-storages>=1.14,<2

